{
  "name": "@repo/db",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db execute --file prisma/reset-demo-schema.sql && prisma db execute --file prisma/enable-pgvector.sql && prisma db push --skip-generate && prisma db execute --file prisma/product-embedding-index.sql",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:dev": "npx prisma migrate dev && npx prisma generate",
    "db:seed": "prisma db seed"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "eslint": "^8.57.0",
    "prisma": "^5.10.2",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.2",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
